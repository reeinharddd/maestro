# openspec/config.yaml — SDD project configuration
# Auto-generated by sdd-init on 2026-06-02
schema: spec-driven

context: |
  Product vision: central orchestrator for AI dev environment (docs/VISION.md)
  Roadmap: Phase 1 (central config DB) → Phase 2 (project scanner) → Phase 3 (daemon proxy)
  Phase 1 scope: complete CRUD all entities, model classification (provider+public), sync debug, export refinement
  Tech stack: Go 1.25.0 CLI tool, Cobra, modernc.org/sqlite, golang-migrate
  Architecture: cmd/maestro/ (main), internal/ (business logic), pkg/models/ (shared types)
  Testing: go test ./..., go vet ./..., go test -race -coverprofile=coverage.out ./...
  Style: Table-driven tests, external test packages, interface-based mocking (no framework)
  Conventions: Conventional commits, TDD vertical slices, SQLite in-memory for tests

rules:
  proposal:
    - Include rollback plan for risky changes
  specs:
    - Use Given/When/Then for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
  design:
    - Include sequence diagrams for complex flows
    - Document architecture decisions with rationale
  tasks:
    - Group by phase, use hierarchical numbering
    - Keep tasks completable in one session
  apply:
    - Follow existing Go idioms and code patterns
    tdd: true
    test_command: "go test -count=1 -run {{.TestName}} ./{{.Pkg}}/"
  verify:
    test_command: "go test -race -coverprofile=coverage.out ./..."
    build_command: "go build ./..."
    coverage_threshold: 60
  archive:
    - Warn before merging destructive deltas
